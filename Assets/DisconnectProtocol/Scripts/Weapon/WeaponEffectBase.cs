using UnityEngine;

public abstract class WeaponEffectBase : MonoBehaviour
{
    // protected WeaponTransformManager manager;

    // protected virtual void Start()
    // {
    //     manager = GetComponent<WeaponTransformManager>();
    // }

    // protected void UpdatePosition(Vector3 offset)
    // {
    //     manager.UpdatePosition(offset);
    // }

    // protected void UpdateRotation(Quaternion offset)
    // {
    //     manager.UpdateRotation(offset);
    // }

    // protected void UpdateTargetPosition (Vector3 targetPosition)
    // {
    //     manager.UpdateTargetPosition(targetPosition);
    // }
}
